apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streaker.fullname" . }}-test-connection"
  labels:
    {{- include "streaker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streaker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
